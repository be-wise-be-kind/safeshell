# SafeShell

Command-line safety layer for AI coding assistants.

SafeShell intercepts shell commands from AI tools, evaluates them against configurable policies, and enforces decisions before execution.
